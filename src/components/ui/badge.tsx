import type { HTMLAttributes } from "react";
import { cn } from "@/lib/utils";
const tones = { slate: "bg-slate-100/80 text-slate-700 ring-slate-200/80", blue: "bg-sky-50 text-sky-700 ring-sky-200/80", amber: "bg-amber-50 text-amber-700 ring-amber-200/80", violet: "bg-violet-50 text-violet-700 ring-violet-200/80", cyan: "bg-cyan-50 text-cyan-700 ring-cyan-200/80", emerald: "bg-emerald-50 text-emerald-700 ring-emerald-200/80", rose: "bg-rose-50 text-rose-700 ring-rose-200/80" };
export function Badge({ className, tone = "slate", ...props }: HTMLAttributes<HTMLSpanElement> & { tone?: keyof typeof tones }) { return <span className={cn("inline-flex items-center rounded-full px-2 py-0.5 text-[11px] font-semibold ring-1", tones[tone], className)} {...props} />; }
export function statusTone(status: string): keyof typeof tones { const s = status.toLowerCase(); if (s.includes("closed") || s.includes("cancel")) return "slate"; if (s === "ok" || s.includes("resolve") || s.includes("healthy") || s.includes("active")) return "emerald"; if (s.includes("wait") || s.includes("warning") || s.includes("risk") || s.includes("progress")) return "amber"; if (s.includes("critical") || s.includes("breach") || s.includes("exceed")) return "rose"; return "blue"; }
